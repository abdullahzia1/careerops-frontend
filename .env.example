# Leave empty to use Vite dev proxy to the API (see vite.config.ts).
# Or set a full origin to call the backend directly (requires CORS match on the API):
# VITE_API_ORIGIN=http://127.0.0.1:3001

VITE_API_ORIGIN=
